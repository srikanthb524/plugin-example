import { PtClassDirective } from './pt-class.directive';

describe('PtClassDirective', () => {
  it('should create an instance', () => {
    const directive = new PtClassDirective();
    expect(directive).toBeTruthy();
  });
});
